spring.application.name=citas-psicologia-app

#Configuración del servidor (puerto 8090 para evitar conflictos)
server.port=8090

# Configuración de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/db_citas_psicologia?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Actualiza las tablas automáticamente
spring.jpa.show-sql=true